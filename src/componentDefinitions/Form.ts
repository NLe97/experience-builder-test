import type { ComponentDefinition } from '@contentful/experience-builder';

const formComponentDefinition: ComponentDefinition = {
  id: 'formExample',
  name: 'Form Example',
  variables: {}
};

export default formComponentDefinition;